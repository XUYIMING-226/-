# 知行册 · 行测学习工作台

一个无需云账号、无需 API Key 的本地可交互原型，当前聚焦「资料分析」。

## 直接运行

用浏览器打开 `index.html` 即可。为获得更稳定的本地文件体验，也可以在本目录运行：

```powershell
python -m http.server 4173
```

然后访问 `http://localhost:4173`。

## 已覆盖的原型流程

- 今日学习概览、连续学习、待复习队列、弱项与用时统计
- 材料与 4 道关联小题作为一个题组展示和保存
- 整份 PDF / 单题截图录入入口（当前为本地模拟解析）
- AI 分类建议必须由学习者确认后才能保存
- 计时重做、错因笔记、速算方法提示和间隔复习
- 资料分析知识图谱；预留数量关系、言语理解、判断推理导航
- 内嵌 AI 学习助手（本地固定回复，不连接任何外部模型）
- 示例保存状态使用浏览器 `localStorage`

## 后续上云需要用户准备

原型不需要任何凭据，也不会上传文件。正式版建议用户自行注册并提供以下国内云资源：

1. 国内对象存储：用于原始 PDF、截图和解析后的页面图片。
2. 国内托管数据库：用于题组、小题、标签、笔记、复习计划、作答记录。
3. 可选 OCR / 大模型服务：用于试卷拆题和分类建议；产品逻辑仍需保留“用户确认后保存”。
4. 身份认证与域名备案方案：若仅个人使用，可先采用单用户登录；正式公网服务再按供应商要求配置。

建议的数据关系是 `MaterialGroup → Question[4] → Attempts / Notes / ReviewSchedule`，避免材料与关联小题在导入或复习时被拆散。

## 文件说明

- `index.html`：页面结构和弹窗
- `styles.css`：完整视觉与响应式布局
- `app.js`：视图切换、计时器、上传模拟、AI 确认和本地助手交互

此版本没有付费依赖、外部账号变更、网络请求或部署动作。
